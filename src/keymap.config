// Not recommended to run keymapper as root,
// Provide full cmdline e.g /usr/bin/jolla-camera

name = "TEST CONFING";
keymap = {
        assist_button = {
                proc = "Camera Starting..";
                trigger = 457;
                cmd = "/user/bin/jolla-camera";
        };
};